const superHeros: string[] = [
  "ironman",
  "spiderman",
  "captain america",
  "batman",
  "hulk",
];

superHeros.push("black widow");
console.log(superHeros);

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({ name: "sovan", isActive: false });

const MLmoldes: number[][] = [[255, 255, 255], []];
